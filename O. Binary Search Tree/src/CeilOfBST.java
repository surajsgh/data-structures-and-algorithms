public class CeilOfBST {
    /*
    //  Time Complexity : O(h)
    //  Auxiliary Space : O(1)
    public static int floor(Node4 root, int x) {
        Node4 res = null;
        while (root!=null) {
            if (root.key==x) {
                return root.key;
            }
            else if (root.key<x) {
                root = root.right;
            }
            else {
                res = root;
                root = root.left;
            }
        }
        return res.key;
    }
     */
}
